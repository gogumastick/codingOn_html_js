<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>axious GET으로 정보받기</h3>
    <form name="register-form">
        <label for="username">이름</label>
        <input type="text" id="username" required />
        <br />
        <fieldset>
            <legend>성별</legend>
            <input type="radio" id="gender" value="남자" />
            <label for="male">남자</label>
            <input type="radio" id="gender" value="여자" />
            <label for="female">여자</label>
        </fieldset>
        <br />
        <fieldset>
            <legend>생년월일</legend>
            <select id="birthYear">
                <% for(let i = 1980; i <= 2020; i++) { %>
                <option value="<%= i  %>"><%= i %></option>
                <% } %>
            </select>
            <label for="birthYear">년</label>
            <select id="birthMonth">
                <% for(let i = 1; i <= 12; i++) { %>
                <option value="<%= i  %>"><%= i %></option>
                <% } %>
            </select>
            <label for="birthMonth">월</label>
            <select id="birthDay">
                <% for(let i= 1; i<=31; i++) { %>
                <option value="<%= i  %>"><%= i %></option>
                <% } %>
            </select>
            <label for="birthDay">일</label>
        </fieldset>
        <br />
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" id="hobby" value="여행" />
            <label for="travel">여행</label>
            <input type="checkbox" id="hobby" value="패션" />
            <label for="fashion">패션</label>
            <input type="checkbox" id="hobby" value="음식" />
            <label for="food">음식</label>
        </fieldset>
        <br />
        <button type="button" onclick="axiosGet()">회원가입</button>
    </form>

    <div class="result"></div>

    <script>
        
        
    </script>
</body>
</html>